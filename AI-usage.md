# AI-usage.md

## 1. באילו כלי AI השתמשתי?

- **Kiro** — עבודה על הקוד (IDE עם AI מובנה).
- **Claude Code** — הכלי המרכזי בתרגיל הזה: תכנון, מימוש (Backend + Frontend), ודיון על שאלות הארכיטקטורה (חלקים ב׳ ו-ג׳).

## 2. באילו שלבים?

בכל השלבים, אבל בצורה שונה בכל אחד:

- **תכנון (Plan)** — לפני כתיבת קוד, ביקשתי מ-Claude Code להיכנס למצב Plan ולהוציא מסמך תכנון (קבצים שישתנו, מבנה ה-API, מודל הנתונים לחיפוש/סינון, מבנה ה-Frontend). עברתי על התכנון, שאלתי שאלות הבהרה, וביקשתי שינויים לפני שאישרתי מימוש.
- **מימוש** — עבדתי מול הצ'אט בפרומפטים קצרים וממוקדים (למשל: "תוסיף חיווי לקלט לא תקין בשדה Request number"), לא פרומפט ענק אחד שמבקש הכול. אחרי כל שינוי עברתי על הקוד שנוצר.
- **בדיקות** — הרצתי את הבדיקות שה-AI כתב, ובנוסף בדקתי ידנית מול המערכת הרצה (curl ל-API, אינטראקציה בדפדפן עם ה-Frontend) — לא הסתפקתי בזה שה-unit tests עברו.
- **Architecture (חלקים ב'+ג')** — לא רק ביקשתי מה-AI להפיק את התרשים הסופי. דיברתי איתו גם על השאלות שמאחורי העיצוב עצמו: מתי תקשורת בין שירותים צריכה להיות סינכרונית ומתי אסינכרונית, איך לשמור על אמינות כשמפרסמים אירועים (Outbox), ואיך כל שירות אמור לגדול תחת עומס (Scaling). בנושאים האלה גם השוויתי בין התשובות של כמה מודלים שונים, כדי לא להסתמך על דעה של מודל אחד בלבד.

## 3. אם השתמשתי ב-AI לתכנון — מה שיניתי מההצעה שקיבלתי, ולמה?

**דוגמה מרכזית — Frontend state management:** ההצעה הראשונית של ה-AI לתכנון ה-Frontend כללה שימוש ב-**NgRx** לניהול state. ביקשתי לממש במקום זאת עם **Signals** ו-**Standalone Components** (ללא NgRx). הסיבה: האפליקציה היא עמוד חיפוש יחיד עם state מקומי פשוט (פילטרים, מיון, עמוד נוכחי, תוצאה) — NgRx מוסיף boilerplate (actions/reducers/effects/selectors) בלי הצדקה אמיתית בהיקף הזה. Signals נותנים reactivity מספיק טוב עם הרבה פחות קוד, וזה גם עקבי עם הכיוון העדכני של Angular.

## 4. האם היו הצעות שדחיתי? דוגמה + הסבר.

**כן — שכבת Repository / Unit of Work נוספת מעל EF Core.** ה-AI הציע להוסיף שכבת הפשטה נפרדת (Repository + Unit of Work) מעל ה-`DbContext`, כ"פרקטיקה נכונה" לפרויקט מקצועי. דחיתי את ההצעה: `DbContext` וה-`DbSet` של EF Core כבר **הם עצמם** מימוש של Repository ו-Unit of Work — `DbSet` הוא ה-Repository, ו-`SaveChanges` הוא ה-Unit of Work. עטיפה נוספת מעליהם היא כפילות שמוסיפה קוד ותחזוקה בלי ערך אמיתי, ומקשה על ניצול מלא של יכולות EF Core (כמו בניית `IQueryable` דינמי לסינון/מיון) דרך שכבת הפשטה מיותרת. השתמשתי ב-EF Core ישירות — זו הדרך הנכונה והמקובלת יותר, לא קיצור דרך.

## 5. כיצד וידאתי שהתוצר נכון?

- **הרצה בפועל, לא רק "זה מתקמפל":** הרצתי את ה-Backend (`dotnet run`) ואת ה-Frontend (`npm start`) יחד, ובדקתי מול המערכת החיה — כולל curl ישיר ל-API עם קומבינציות פילטרים תקינות ולא-תקינות, ואינטראקציה בדפדפן (חיפוש, מיון, דפדוף, מעבר בין משתמש רגיל למנהל).
- **בדיקות קצה:** ניסיתי במכוון קלט "רע" (טקסט בעברית בשדה Request Number, טווח תאריכים הפוך, ערך Status לא קיים) כדי לוודא שהחיווי לשגיאות עובד בפועל ולא רק בתיאוריה.
- **קריאת קוד, לא רק קבלה אוטומטית:** עברתי שורה-שורה על כל קובץ שנוצר לפני שהמשכתי הלאה, ווידאתי שאני מבין/ה ויכול/ה להסביר כל החלטה (כנדרש בכללי המבחן).
- **בדיקות יחידה (17 Backend, 15 Frontend)** — כיסוי למקרים המשמעותיים: הרשאות (admin/regular), כל סוגי הסינון, מיון עם תיקו, pagination, ווולידציה של קלט לא תקין.
